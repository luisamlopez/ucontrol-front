import { Box } from "@mui/material";
import { Device } from "../../api/Device";

const ACCMobileDevice = (device: { device: Device }): JSX.Element => {
  return <Box>movil</Box>;
};

export default ACCMobileDevice;
